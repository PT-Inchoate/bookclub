import React from 'react';
import { Default } from 'react-spinners-css';

const Spinner = () => (
    <div className="Spinner-container">
        <Default color="#C7E2FD" size={70} />
    </div>
)
  
export default Spinner;